name: "Create artifact"

on:
    push:
        branches:
            - master
    workflow_dispatch:

jobs:
    quickbuild:
        name: Quick build
        uses: navikt/crm-workflows-base/.github/workflows/ciPipeline.yml@dxAtScale
        permissions:
            contents: read
            packages: write
        secrets: inherit