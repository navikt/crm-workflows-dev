name: "[PUSH] Create artifact"
on:
    push:
    workflow_dispatch:
jobs:
    quickbuild:
        name: Quick build
        uses: navikt/crm-workflows-base/.github/workflows/ciPipeline.yml@dxAtScale
        permissions:
            contents: read
            packages: write
        secrets: inherit